<h1 class="title">This is the Home page</h1>
<span>I as trying to make dynamic changes on the pages, not focus on the content...</span>

<div class="home-container">
  {{#each db}}
    <a href="{{this.name}}" class="card">
        <h2 class="sub">{{this.name}}</h2>
    </a>
  {{/each}}
</div>